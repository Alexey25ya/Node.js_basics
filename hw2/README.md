# Alexey random functions

genPassword(length of password) - random password generation function. Input data - password length

randomInteger(min, max) - Function for generating a random integer in a given range.